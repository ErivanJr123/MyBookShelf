doctype html
html(lang="pt-br")
  head
    title MyBookShelf
    link(rel="stylesheet", href="/css/style.css")
    style.
      /* Efeito visual moderno de vibração para quando o erro surge */
      @keyframes shake {
        0% { transform: translateX(0); }
        25% { transform: translateX(-10px); }
        50% { transform: translateX(10px); }
        75% { transform: translateX(-10px); }
        100% { transform: translateX(0); }
      }
  body
    nav
      a(href="/") 🏠 Home
      a(href="/authors") ✍️ Autores
      a(href="/books") 📚 Livros
      
    .container
      // 1. A caixinha de erro global fica centralizada aqui, no topo do conteúdo
      #msg-global-erro(style="display: none; background: #ffcccc; color: #cc0000; border: 1px solid #cc0000; padding: 15px; margin-bottom: 20px; border-radius: 8px;")
      
      // 2. O conteúdo específico de cada página entra exatamente aqui abaixo
      block content

    // 3. Importação do seu arquivo centralizador de requisições
    script(src="/js/apiClient.js")
    
    // 4. Código real das funções de exibir e limpar o erro na tela
    script.
      window.mostrarErroGlobal = function(mensagem) {
        const caixa = document.getElementById('msg-global-erro');
        if (caixa) {
          caixa.innerText = `❌ Erro: ${mensagem}`;
          caixa.style.display = 'block';
          caixa.style.animation = "shake 0.5s";
          setTimeout(() => caixa.style.animation = "", 500);
        }
      };
      
      window.limparErroGlobal = function() {
        const caixa = document.getElementById('msg-global-erro');
        if (caixa) {
          caixa.style.display = 'none';
        }
      };
